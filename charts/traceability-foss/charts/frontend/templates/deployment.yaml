apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.frontend.namespace | default .Release.Namespace }}
  name: {{ include "traceability-foss-frontend.fullname" . }}
  labels:
    {{- include "traceability-foss-frontend.labels" . | nindent 4 }}
spec:
  {{- if not .Values.frontend.autoscaling.enabled }}
  replicas: {{ .Values.frontend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "traceability-foss-frontend.selectorLabels" . | nindent 6 }}
  revisionHistoryLimit: 2
  template:
    metadata:
      namespace: {{ .Values.frontend.namespace | default .Release.Namespace }}
      {{- with .Values.frontend.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "traceability-foss-frontend.selectorLabels" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      {{- if .Values.frontend.imagePullSecret.dockerconfigjson }}
      imagePullSecrets:
        - name: {{ include "traceability-foss-frontend.fullname" . }}-imagepullsecret
      {{- else }}
      {{- with .Values.frontend.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      serviceAccountName: {{ include "traceability-foss-frontend.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.frontend.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.frontend.securityContext | nindent 12 }}
          image: "{{ .Values.frontend.image.repository }}-frontend:{{ .Values.frontend.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          env:
            - name: CATENAX_PORTAL_API_URL
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_API_URL }}"
            - name: CATENAX_PORTAL_KEYCLOAK_URL
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_KEYCLOAK_URL }}"
            - name: CATENAX_PORTAL_CLIENT_ID
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_CLIENT_ID }}"
            - name: CATENAX_PORTAL_BASE_URL
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_BASE_URL }}"
            - name: CATENAX_PORTAL_BACKEND_DOMAIN
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_BACKEND_DOMAIN }}"
            - name: CATENAX_PORTAL_URL
              value: "{{ .Values.frontend.image.CATENAX_PORTAL_URL }}"
          ports:
            - name: http
              containerPort: {{ .Values.frontend.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
      {{- with .Values.frontend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
